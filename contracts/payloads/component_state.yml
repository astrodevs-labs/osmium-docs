type: object
properties:
  id:
    type: string
    description: Unique ID of the component.
  state:
    type: string
    description: The state event that occurred.
  data:
    type: array
    description: Optionnal event data (e.g key inputs).
